apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqs-admin.fullname" . }}
  labels:
    {{- include "sqs-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqs-admin.selectorLabels" . | nindent 4 }}
