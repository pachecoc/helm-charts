apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqs-ui.fullname" . }}
  labels:
    {{- include "sqs-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqs-ui.selectorLabels" . | nindent 4 }}
